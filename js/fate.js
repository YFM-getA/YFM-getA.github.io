/*
 * js for fate.
 */
 var init = function a() {
/*    var month_div = ele('#monthUl');
    for (var i = 0 ; i < 12; i++) {
        var dom = document.createElement('li');
        dom.appendChild(document.createTextNode(MONTH_NAME[i]));
        month_div.appendChild(dom);
    }*/
}
